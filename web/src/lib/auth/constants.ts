// Where a user lands after completing sign-up/login. Kept in one place so
// the client auth component and the /auth/callback route agree on it.
export const DEFAULT_REDIRECT_PATH = "/listings/new";
